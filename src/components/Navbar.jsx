import { appleImg } from "../utils"
const Navbar = () => {
    return (
        <header>
            <nav>
                <img src = {appleImg}/>
            </nav>
        </header>
    )
}

export default Navbar